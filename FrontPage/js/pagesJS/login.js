document.addEventListener('DOMContentLoaded', function(){    
    //Inicializamos nuestra pagina con este evento
    let init = loginInit();
    //Inicializamos los eventos de nuestra pagina
    let events = addEvents();    
});